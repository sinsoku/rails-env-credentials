RSpec.describe Rails::Env::Credentials do
  it "has a version number" do
    expect(Rails::Env::Credentials::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
